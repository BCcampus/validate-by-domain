<?xml version="1.0"?>
<ruleset>
    <!-- Scan only PHP files -->
    <arg name="extensions" value="php"/>
    <!-- Show colors in console -->
    <arg value="-colors"/>
    <!-- Show progress -->
    <arg value="p"/>
    <!-- Be quiet -->
    <arg value="q"/>
    <!-- Include source codes in the report -->
    <arg value="s"/>
    <!-- Use HM Coding Standards -->
    <rule ref="vendor/humanmade/coding-standards">
        <!-- Disable all ESLint checks -->
        <exclude name="HM.Debug.ESLint"/>
        <!-- Disable rules we disagree with -->
        <exclude name="WordPress.NamingConventions.ValidFunctionName.MethodNameInvalid"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName.MemberNotSnakeCase"/>
        <exclude name="WordPress.NamingConventions.ValidVariableName.NotSnakeCaseMemberVar"/>
        <exclude name="WordPress.VIP.SessionVariableUsage"/>
        <exclude name="WordPress.VIP.SessionFunctionsUsage"/>
        <!-- Disable LayoutOrder until humanmade/coding-standards#5 is fixed -->
        <exclude name="HM.Layout.Order.WrongOrder"/>
    </rule>

    <!-- Re-enable rules we agree with -->
    <rule ref="Generic.NamingConventions.UpperCaseConstantName"/>

    <exclude-pattern>vendor/*</exclude-pattern>
    <exclude-pattern>tests/*</exclude-pattern>

    <!-- justfication is that we're hooking into sign up events which check nonces -->
    <rule ref="WordPress.CSRF.NonceVerification.NoNonceVerification">
        <exclude-pattern>inc/class-validate.php</exclude-pattern>
        <exclude-pattern>inc/class-honeypot.php</exclude-pattern>
    </rule>

</ruleset>
